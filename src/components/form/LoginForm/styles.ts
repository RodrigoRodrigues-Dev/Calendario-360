import styled from 'styled-components';
import { FormContainerStyled } from '../FormContainer/styled';

export const AuthLandingLogin = styled(FormContainerStyled)`
  gap: 2.5rem;
`;

export const AuthLandingLoginHeader = styled(FormContainerStyled)`
  gap: 1rem;
`;
