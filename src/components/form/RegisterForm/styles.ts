import styled from 'styled-components';
import { FormContainerStyled } from '../FormContainer/styled';

export const AuthLandingRegister = styled(FormContainerStyled)`
  gap: 1rem;
`;

export const AuthLandingRegisterHeader = styled(FormContainerStyled)`
  gap: 1rem;
  margin-bottom: 1rem;
`;
